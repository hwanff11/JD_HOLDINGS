name: JDSS Multi Sleeve Research

on:
  push:
    branches: [research/jdss-simple-strategies]
    paths:
      - scripts/research_multi_sleeve.py
      - scripts/research_simple_strategies.py
      - .github/workflows/jdss-multi-sleeve-research.yml
  pull_request:
    paths:
      - scripts/research_multi_sleeve.py
      - scripts/research_simple_strategies.py
      - .github/workflows/jdss-multi-sleeve-research.yml
  workflow_dispatch:
    inputs:
      end_date:
        description: Last completed U.S. trading session (YYYY-MM-DD)
        required: true
        default: "2026-08-10"

permissions:
  contents: read

concurrency:
  group: jdss-multi-sleeve-${{ github.ref }}
  cancel-in-progress: true

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        slippage: ["0.0005", "0.001", "0.002"]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e '.[dev]'
      - name: Lint research runner
        run: ruff check scripts/research_multi_sleeve.py
      - name: Run multi-sleeve research
        env:
          END_DATE: ${{ inputs.end_date || '2026-08-10' }}
          SLIPPAGE: ${{ matrix.slippage }}
        run: |
          python scripts/research_multi_sleeve.py \
            --end "$END_DATE" \
            --slippage "$SLIPPAGE" \
            --output reports/multi_sleeve.json \
            --markdown reports/multi_sleeve.md
      - name: Publish job summary
        run: cat reports/multi_sleeve.md >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v6
        with:
          name: jdss-multi-sleeve-${{ github.run_id }}-${{ matrix.slippage }}
          path: |
            reports/multi_sleeve.json
            reports/multi_sleeve.md
          retention-days: 30
