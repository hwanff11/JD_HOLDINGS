name: Research Backtest

on:
  pull_request:
    paths:
      - "scripts/backtest_v2_final_stress.py"
      - "scripts/backtest_v2_remainder_exit.py"
      - "scripts/backtest_v2_focus.py"
      - "src/jd_holdings/backtest/**"
      - "src/jd_holdings/core/**"
      - "strategy.yaml"
      - ".github/workflows/research-backtest.yml"

permissions:
  contents: read

jobs:
  final-stress:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e '.[dev]'
      - run: python scripts/backtest_v2_final_stress.py --output reports/v2_final_stress.json
      - uses: actions/upload-artifact@v4
        with:
          name: jdss-v2-final-stress
          path: |
            reports/v2_final_stress.json
            reports/v2_final_stress.md
