name: Research Backtest
on:
  pull_request:
    paths:
      - "scripts/backtest_v2_score_grid.py"
      - "scripts/backtest_v2_remainder_exit.py"
      - "scripts/backtest_v2_focus.py"
      - "src/jd_holdings/backtest/**"
      - "src/jd_holdings/core/**"
      - "strategy.yaml"
      - ".github/workflows/research-backtest.yml"
permissions:
  contents: read
jobs:
  score-grid:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e '.[dev]'
      - run: python scripts/backtest_v2_score_grid.py --output reports/v2_score_grid.json
      - uses: actions/upload-artifact@v4
        with:
          name: jdss-v2-score-grid
          path: |
            reports/v2_score_grid.json
            reports/v2_score_grid.md
