name: Research Backtest

on:
  pull_request:
    paths:
      - "scripts/backtest_v2_main_vs_final.py"
      - "scripts/backtest_v2_remainder_exit.py"
      - "scripts/backtest_v2_focus.py"
      - "src/jd_holdings/backtest/**"
      - "src/jd_holdings/core/**"
      - "strategy.yaml"
      - ".github/workflows/research-backtest.yml"

permissions:
  contents: read

jobs:
  main-vs-final:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e '.[dev]'
      - run: python scripts/backtest_v2_main_vs_final.py --output reports/v2_main_vs_final.json
      - uses: actions/upload-artifact@v4
        with:
          name: jdss-v2-main-vs-final
          path: |
            reports/v2_main_vs_final.json
            reports/v2_main_vs_final.md
